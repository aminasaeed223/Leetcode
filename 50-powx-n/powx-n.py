class Solution:
    def myPow(self, x: float, n: int) -> float:
        # Simplpy returning the power of given no of inputs
        return pow(x,n)
        y = float(x**n)
        return y
        

        